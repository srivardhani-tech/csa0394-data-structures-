#include <stdio.h>
int fact(int y);
int main()
{
int x;
printf("enter the number\n");
scanf("%d",&x);
printf("The factorial of the number is %d", fact(x));
return 0;
}
int fact(int y)
{
if (y==0||y==1)
{
return 1;
}
else
{
return y * fact(y-1);
}
}
